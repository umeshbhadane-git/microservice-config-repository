spring:
  application:
    name: USER-CLIENT

  datasource:
    url: jdbc:mysql://localhost:3306/User_Microservice
    username: root
    password: Umesh123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQLDialect
        format_sql: true

logging:
  level:
    com.successive.UserService.service: trace

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081 