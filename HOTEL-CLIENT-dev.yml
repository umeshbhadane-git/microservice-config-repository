spring:
  application:
    name: HOTEL-CLIENT

  datasource:
    url: jdbc:postgresql://localhost:5433/Hotel_Microservice
    username: postgres
    password: Umesh123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.PostgreSQLDialect
        format_sql: true


server:
  port: 8082

logging:
  level:
    com.successive.HotelService.service: trace

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

